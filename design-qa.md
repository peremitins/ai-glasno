# Visual QA — «Новая репетиция»

## Проверка

- [x] Сопоставлены эталонный скриншот и локальный экран в desktop-ширине: боковая навигация, две контекстные колонки, плотные группы параметров и блок «Свои вопросы» сохранены.
- [x] Комбобокс роли открыт в браузере, остаётся доступным после клика и закрывается после выбора или клика вне поля.
- [x] Раскрытое меню роли имеет непрозрачный контрастный фон (`rgb(32, 38, 58)`), поэтому пункты и их описания остаются читаемыми поверх страницы.
- [x] Загрузка резюме показывает загрузочное состояние, имя файла, предпросмотр извлечённого текста и действие очистки; заметки кандидата не затираются.
- [x] «Свои вопросы» использует переиспользуемый VoiceTextarea с авторазмером, очисткой и расположением управляющих элементов как в исходной форме.
- [x] Общая кнопка голосового ввода работает в обеих формах и в интерфейсе репетиции: меняет доступное имя и состояние, запускает/останавливает распознавание речи, добавляет итоговую фразу в поле и анимируется во время записи.
- [x] Локальная Vite-разработка обрабатывает загрузки резюме и списка вопросов до проксирования: тестовый `POST` к обоим маршрутам возвращает извлечённый текст вместо 404.
- [x] Параметры визуально сверены с пользовательским снимком: заголовок и поясняющий текст карточки расположены в отдельных строках без наложения.
- [x] Проверены основной сценарий, прикрепление резюме/вопросов и создание сессии: 98 тестов пройдены; клиентская и серверная production-сборки пройдены.

## Итог

final result: passed
