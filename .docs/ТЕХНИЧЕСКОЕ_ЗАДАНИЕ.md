# Гласно — техническое задание и план реализации

> **Для исполнителей:** выполнять этапы последовательно; завершать каждый этап проверкой и одним атомарным коммитом.

**Цель:** создать веб-приложение «Гласно» на React для проведения и разбора профессиональных диалогов по вакансии и профилю кандидата.

**Архитектура:** одностраничное React-приложение на Vite с модульной структурой. Состояние интерфейса и доменных сценариев хранится в Redux Toolkit, асинхронные запросы и кэширование — в RTK Query. До подключения защищённого API используется типизированный mock API через MSW; ключи провайдеров и платёжные данные в браузер не передаются.

**Технологии:** React 19, TypeScript, Vite, React Router, Redux Toolkit, React Redux, RTK Query, MSW, React Hook Form, Zod, i18next, react-i18next, Tailwind CSS, shadcn/ui, Vitest, React Testing Library, ESLint и Prettier.

---

## 1. Границы продукта

Приложение работает в современных браузерах и Telegram WebApp. Первичный язык интерфейса — русский; пользовательские строки хранятся в словаре локализации. При недоступности браузерных возможностей приложение показывает понятный текстовый сценарий вместо ошибки.

В состав первой версии входят:

- вход и демо-сессия пользователя;
- дашборд с краткой статистикой и быстрым стартом;
- создание сессии: вакансия, профиль, уровень, формат и настройки вопросов;
- проведение сессии: вопрос, ответ, таймер, подсказки, текстовый и голосовой ввод;
- история сессий и подробный отчёт;
- профиль, настройки и тарифный экран;
- каталог вопросов и страница вопроса;
- состояния загрузки, пустых данных, ошибок и отсутствующих разрешений браузера.

В первую версию не входят реальная оплата, хранение персональных файлов, отправка сообщений, вызовы AI-провайдеров из браузера и передача секретов в клиентский код.

## 2. Маршруты

| Маршрут                 | Экран            | Основная ответственность                                 |
| ----------------------- | ---------------- | -------------------------------------------------------- |
| `/`                     | Дашборд          | Статистика, быстрый запуск, последние сессии             |
| `/auth`                 | Вход             | Валидация формы и создание демо-сессии                   |
| `/interview/new`        | Новая сессия     | Параметры диалога и создание сессии                      |
| `/interview/:id`        | Сессия           | Вопросы, ответы, таймер, подсказки и голосовой интерфейс |
| `/interview/report/:id` | Отчёт            | Навыки, рекомендации, термины и результаты               |
| `/history`              | История          | Список и фильтрация завершённых сессий                   |
| `/profile`              | Профиль          | Данные пользователя и настройки                          |
| `/pricing`              | Тарифы           | Планы и ограничение функций без платёжной интеграции     |
| `/questions`            | Каталог вопросов | Поиск и фильтрация вопросов                              |
| `/questions/:slug`      | Вопрос           | Детали вопроса и материалы                               |
| `*`                     | 404              | Переход к доступному маршруту                            |

## 3. Архитектура исходного кода

```text
src/
  app/                 # провайдеры, router, store, глобальные стили
  pages/               # компоненты маршрутов
  widgets/             # композиции для экранов: sidebar, dashboard, session workspace
  features/            # пользовательские действия и самостоятельные сценарии
  entities/            # типы и отображение: session, question, report, profile
  shared/
    api/               # RTK Query API, типы транспорта, обработка ошибок
    config/            # константы и публичные настройки
    lib/               # независимые утилиты
    ui/                # shadcn/ui и прикладные UI-элементы
  mocks/               # MSW handlers и согласованные тестовые данные
  i18n/                # конфигурация и словари локализации
  test/                # настройки тестовой среды и тестовые фабрики
```

Границы зависимостей: `pages` собирают `widgets` и `features`; `features` используют `entities` и `shared`; нижние слои не импортируют верхние. Компонент не обращается к сети напрямую: получение и изменение данных выполняются через хуки RTK Query.

## 4. Состояние и данные

### Redux Toolkit

Store содержит только синхронное клиентское состояние:

- `auth`: демо-сессия, статус входа и пользователь;
- `interview`: выбранные параметры, текущий ответ, статус этапа и таймер;
- `ui`: меню, модальные окна, уведомления и предпочтения отображения.

Состояние изменяется только reducer-ами `createSlice`; компоненты читают узкие selectors. Сложные последовательности действий оформляются в listener middleware или в отдельные функции feature-слоя, а не в JSX.

### RTK Query и mock API

RTK Query описывает endpoints для профиля, дашборда, сессий, отчётов, тарифов и каталога вопросов. MSW перехватывает запросы в режиме `mock`, возвращает типизированные ответы и моделирует задержку, пустой результат и прикладную ошибку. В режиме интеграции базовый URL берётся только из `VITE_API_BASE_URL`.

Каждый endpoint имеет типы входа и ответа, понятные tag types для invalidation и обработку состояний `isLoading`, `isFetching`, `isError` и `data` в интерфейсе.

## 5. Формы, разрешения и доступность

- Все формы используют React Hook Form и Zod; сообщения валидации локализованы.
- Для микрофона, камеры и полноэкранного режима предусмотрена проверка доступности API через `typeof` и отдельный UI для отказа в разрешении.
- Голосовой ввод имеет обязательный текстовый fallback.
- Интерактивные элементы доступны с клавиатуры, имеют корректные aria-атрибуты и видимый фокус.
- Цвета определяются CSS-переменными и токенами Tailwind; контраст и уменьшение анимаций учитываются через системные предпочтения.

## 6. Безопасность

- В репозитории не хранятся реальные токены, приватные ключи, персональные данные и ответы внешних сервисов.
- В клиентский код не добавляются ключи AI-провайдеров, relay-секреты или секреты платежей.
- `.env` не коммитится; в `.env.example` допускаются только имена публичных переменных без значений.
- Любая будущая защищённая интеграция выполняется через серверный API, а не из браузера.

## 7. Этапы реализации и коммиты

### Этап 0. Проектные правила и ТЗ

- [x] Зафиксировать техническое задание и правила агентской среды.
- [x] Проверить, что документация не содержит секретов и описывает только актуальный стек.
- [x] Коммит: `docs: добавить техническое задание проекта`.

**Критерий приёмки:** в корне есть актуальное ТЗ, а правила для Codex и Claude Code согласованы с React-стеком.

### Этап 1. Основа приложения

- [x] Настроить Tailwind CSS, shadcn/ui, Prettier, Vitest и Testing Library.
- [x] Создать структуру `src/app`, `pages`, `widgets`, `features`, `entities`, `shared`, `mocks` и `i18n`.
- [x] Подключить React Router, общий layout, 404-страницу и lazy loading маршрутов.
- [x] Коммит: `feat: настроить базовую архитектуру приложения`.

**Критерий приёмки:** `pnpm lint`, `pnpm test` и `pnpm build` проходят; все маршруты открываются и имеют skeleton или содержимое.

### Этап 2. Store, API-контракты и mock-слой

- [x] Подключить Redux Toolkit, React Redux и RTK Query.
- [x] Создать typed base API, tag types и MSW handlers.
- [x] Добавить единый формат прикладной ошибки и тестовые фабрики.
- [x] Коммит: `feat: добавить Redux store и mock API`.

**Критерий приёмки:** дашборд получает данные через RTK Query, а тест проверяет loading, success и error состояния.

### Этап 3. Доступ и профиль

- [x] Реализовать экран входа с React Hook Form и Zod.
- [x] Создать `auth` slice, защищённый layout и mock endpoints профиля.
- [x] Реализовать просмотр и редактирование профиля.
- [x] Коммит: `feat: добавить вход и управление профилем`.

**Критерий приёмки:** неавторизованный пользователь перенаправляется на `/auth`; валидная форма создаёт демо-сессию, а профиль сохраняется через mutation.

### Этап 4. Дашборд и история

- [x] Реализовать сводку, последние сессии и быстрый старт.
- [ ] Реализовать историю, фильтрацию и пустое состояние.
- [ ] Покрыть selectors и ключевые UI-состояния тестами.
- [ ] Коммит: `feat: добавить дашборд и историю сессий`.

**Критерий приёмки:** данные не дублируются в slice и RTK Query cache; фильтры отражаются в URL.

### Этап 5. Создание сессии

- [ ] Создать многошаговую форму параметров сессии.
- [ ] Валидировать вакансию, профиль, формат, уровень и настройки вопросов.
- [ ] Сохранить черновик в клиентском состоянии и создать сессию через API mutation.
- [ ] Коммит: `feat: добавить создание сессии`.

**Критерий приёмки:** пользователь не может перейти к следующему шагу с невалидными данными; после создания открывается `/interview/:id`.

### Этап 6. Рабочее пространство сессии

- [ ] Реализовать вопрос, редактор ответа, таймер, навигацию и подсказки.
- [ ] Описать допустимые статусы сессии и переходы между ними в `interview` slice.
- [ ] Добавить сохранение ответа и завершение сессии через RTK Query.
- [ ] Коммит: `feat: добавить рабочее пространство сессии`.

**Критерий приёмки:** обновление страницы сохраняет черновик; недопустимые действия недоступны в UI и не вызывают mutation.

### Этап 7. Голосовой и браузерный UX

- [ ] Реализовать проверку поддержки микрофона, камеры, fullscreen и Web Speech API.
- [ ] Добавить текстовый fallback, диалоги разрешений и локальный preview камеры.
- [ ] Покрыть feature detection unit-тестами.
- [ ] Коммит: `feat: добавить голосовой ввод и браузерные fallback`.

**Критерий приёмки:** приложение остаётся работоспособным в браузере без Web Speech API или при отказе в разрешении.

### Этап 8. Отчёт и каталог вопросов

- [ ] Реализовать отчёт: оценка навыков, рекомендации, термины и дальнейшие действия.
- [ ] Реализовать каталог вопросов, поиск, фильтрацию и страницу вопроса.
- [ ] Добавить error boundaries для маршрутов отчёта и каталога.
- [ ] Коммит: `feat: добавить отчёты и каталог вопросов`.

**Критерий приёмки:** экран отчёта отображает loading, error и success; URL вопроса типобезопасно валидируется router-ом.

### Этап 9. Тарифы и визуальная полировка

- [ ] Реализовать тарифный экран и UI ограничения функций без интеграции оплаты.
- [ ] Проверить адаптивность от 320px, клавиатурную навигацию и reduced motion.
- [ ] Удалить стартовые Vite-ассеты и демо-компоненты.
- [ ] Коммит: `feat: завершить тарифный экран и адаптивный интерфейс`.

**Критерий приёмки:** интерфейс не зависит от жёстко заданных цветов, а интерактивные элементы доступны без мыши.

### Этап 10. Финальная проверка

- [ ] Выполнить `pnpm lint`, `pnpm test` и `pnpm build`.
- [ ] Обновить README: запуск, архитектура, доступные команды и переменные окружения.
- [ ] Проверить, что в Git нет секретов, `dist`, `node_modules` и временных файлов.
- [ ] Коммит: `docs: описать запуск и архитектуру приложения`.

**Критерий приёмки:** все команды проходят на чистой установке зависимостей, а README соответствует фактической структуре проекта.

## 8. Правила коммитов

Коммит создаётся после законченной, проверяемой задачи. Он содержит одну связанную цель, не смешивает рефакторинг с новой функцией и пишется на русском в формате:

```text
тип: краткое действие в повелительном наклонении
```

Допустимые типы: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`. Отправка коммитов в удалённый репозиторий выполняется только по явному запросу.

## 9. Конфигурация агентской среды

- `AGENTS.md` — общие правила для автоматизированной разработки, безопасность, архитектура и порядок коммитов.
- `CLAUDE.md` и `.claude/settings.json` — правила Claude Code и hooks для Serena.
- `.claude/agents/i18n-sweeper.md` — узкий read-only агент для контроля пользовательских строк после подключения i18next.
- Codex использует общий локальный MCP-конфиг Serena; проектные инструкции читает из `AGENTS.md`.

Узкие read-only задачи выполняются минимальным доступным effort. Сложные изменения сначала ограничиваются точным поиском, проверкой references и чтением только затронутых модулей. Для служебной генерации используются активные подписочные сессии, а не ключи runtime-функций приложения.
